import styled from "styled-components";

const UserStyle = styled.div`
  .profile_pic {
    width: 30px;    
    line-height: 14px;
    img {
      width: 100%;
      border-radius: 50%;
      position: relative;
      // top: 0.5rem;
    }
  }
`;

export default UserStyle;
